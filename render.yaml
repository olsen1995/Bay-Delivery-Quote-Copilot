services:
  - type: web
    name: lifeos-api
    env: python
    pythonVersion: 3.11.9
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn lifeos.main:app --host 0.0.0.0 --port 10000
